{
  "version": 2,
  "build": {
    "command": "npm install --legacy-peer-deps && npm run build"
  },
  "builds": [
    { "src": "app.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/app.js" }
  ]
}
